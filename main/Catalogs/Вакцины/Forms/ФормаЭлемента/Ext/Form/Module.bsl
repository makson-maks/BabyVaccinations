
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ТолькоПросмотр = Истина;
	
	УправлениеФормой();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработкаКомандФормы

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура ИнфекцииПриАктивизацииСтроки(Элемент)
	
	ТекущиеДанные = Элементы.Инфекции.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Ключ", ТекущиеДанные.Инфекция);
	
	ОткрытьФорму("Справочник.Инфекции.ФормаОбъекта", ПараметрыФормы);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыФункции

&НаСервере
Процедура УправлениеФормой()
	
	Элементы.ДекорацияТипВакцины.Заголовок = Объект.ТипВакциныПоКоличествуИнфекцийОтКоторыхОнаЗащищает;
	Элементы.СредняяСтоимость.Видимость = Не Объект.Бесплатная;
	Элементы.ДекорацияБесплатная.Видимость = Объект.Бесплатная;
	Элементы.ДекорацияИмпортная.Видимость = Не Объект.Отечественная;
	Элементы.ДекорацияОтечественная.Видимость = Объект.Отечественная;
	
КонецПроцедуры

#КонецОбласти